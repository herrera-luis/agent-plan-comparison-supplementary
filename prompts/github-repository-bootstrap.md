<!--
Frozen prompt body for the github-repository-bootstrap benchmark.

The prompt is sent verbatim to every (tool, leg) cell. The SHA-256 fingerprint
at the bottom is computed over the byte range strictly between the markers
below (exclusive on both sides):

    awk '/^--- prompt body begins ---$/{flag=1;next} /^--- prompt body ends ---$/{flag=0} flag' \
      research/agent-plan-comparison-final/prompts/github-repository-bootstrap.md \
      | shasum -a 256
-->

--- prompt body begins ---
Add a new Terraform repo file at `infra-terraform/github/repositories/sre/agent_research_eval.tf` defining a GitHub repository named `agent_research_eval` (visibility private, default branch `main`, standard SRE branch protection) by mirroring the structure of the sibling file `infra-terraform/github/repositories/sre/agent_skillzz.tf`. The verbatim contents of that sibling file are:

```hcl
# Autogenerated by script

module "repository_agent_skillzz" {
  source = "git::https://github.com/Cognitiverun/infra-terraform-modules.git//github-repository?ref=v0.2.17"
  #source                 = "../../../infra-terraform-modules/github-repository"
  name                      = "agent-skillzz"
  description               = "Dedicated to host reusable agent skills across cognitiverun teams"
  allow_merge_commit        = false
  auto_init                 = false
  topics                    = ["team-sre"]
  homepage_url              = ""
  visibility                = "private"
  default_branch            = "main"
  archived                  = false
  lock_branch               = local.lock_branch
  squash_merge_commit_title = "COMMIT_OR_PR_TITLE"
  security_and_analysis = {
    advanced_security               = "enabled"
    secret_scanning                 = "enabled"
    secret_scanning_push_protection = "enabled"
  }
  # Permission options are: pull, triage, push, maintain, admin
  team_access = [
    {
      team_id    = local.teams["bots"].team_id
      permission = local.permissions.pull
    },
    {
      team_id    = local.teams["tech_services_network_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["tech_services_network"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["tech_services_engineering_management"].team_id
      permission = local.permissions.pull
    },
    {
      team_id    = local.teams["tech_services_engineering"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["tech_services_support_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["tech_services_operations_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["tech_services_operations"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["tech_services_operations_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["infosec_management"].team_id
      permission = local.permissions.write_bypass_branch_protection
    },
    {
      team_id    = local.teams["infosec_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["infosec"].team_id
      permission = local.permissions.write_bypass_branch_protection
    },
    {
      team_id    = local.teams["infosec_readonly"].team_id
      permission = local.permissions.pull
    },
    {
      team_id    = local.teams["atlas_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["atlas"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["atlas_readonly"].team_id
      permission = local.permissions.pull
    },
    {
      team_id    = local.teams["atlas_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["data_infrastructure_management"].team_id
      permission = local.permissions.write_bypass_branch_protection
    },
    {
      team_id    = local.teams["data_infrastructure"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["data_infrastructure_readonly"].team_id
      permission = local.permissions.pull
    },
    {
      team_id    = local.teams["data_infrastructure_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["data_engineering"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["data_engineering_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["data_science"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["data_science_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["argon_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["argon"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["argon_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["argon_readonly"].team_id
      permission = local.permissions.pull
    },
    {
      team_id    = local.teams["findev_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["findev"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["findev_readonly"].team_id
      permission = local.permissions.pull
    },
    {
      team_id    = local.teams["findev_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["platform_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["platform"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["platform_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["vision_services"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["vision_services_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["wapi"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["wapi_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["control_remediation_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["sre_management"].team_id
      permission = local.permissions.admin
    },
    {
      team_id    = local.teams["sre"].team_id
      permission = local.permissions.admin
    },
    {
      team_id    = local.teams["sre_readonly"].team_id
      permission = local.permissions.pull
    },
    {
      team_id    = local.teams["sre_contractors"].team_id
      permission = local.permissions.admin
    },
    {
      team_id    = local.teams["product_engineering_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["product_engineering"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["product_engineering_readonly"].team_id
      permission = local.permissions.pull
    },
    {
      team_id    = local.teams["product_engineering_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["randd_management"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["randd"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["randd_contractors"].team_id
      permission = local.permissions.push
    },
    {
      team_id    = local.teams["catalog_contractors"].team_id
      permission = local.permissions.push
    },
  ]
  deploy_branch_protection         = true
  branch_protection_enforce_admins = true
  branch_protection_required_status_checks = {
    strict   = true
    contexts = []
  }
  branch_protection_required_pull_request_reviews = {
    dismiss_stale_reviews           = true
    require_code_owner_reviews      = true
    required_approving_review_count = 1
  }
  delete_branch_on_merge     = true
  enable_issues              = true
  enable_downloads           = true
  enable_wiki                = true
  enable_projects            = true
  enable_vulnerabiliy_alerts = true
}
```

Produce a per-task test plan but do not actually write the file into the live `infra-terraform` working tree.
--- prompt body ends ---

<!-- sha256: 3aace6903c10ab6ab2639e6565090e144afe7597caa9d9a72fb254aa761f78d9 -->
